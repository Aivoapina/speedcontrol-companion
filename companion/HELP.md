## Speedcontrol companion

- Make folder named `speedcontrol-companion` inside nodecg bundles folder
- Copy or symbolic link contents of nodecg folder to that folder
- Run `npm install` inside `speedcontrol-companion`
- Start nodecg
